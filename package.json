{
  "name": "colabel",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:migrate": "turbo db:migrate --filter=web",
    "db:generate": "turbo db:generate --filter=web",
    "db:studio": "turbo db:studio --filter=web",
    "db:import": "turbo db:import --filter=web",
    "db:seed": "turbo db:seed --filter=web",
    "hf:export": "turbo hf:export --filter=web"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@10.6.0"
}
